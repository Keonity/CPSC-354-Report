// is_empty

// if list = ENil then 1 else 0

let f = (\x. if x = # then 1 else 0) in f a:b:#

;;

// fib

// let rec f = (\x. (if x = 0 then 0 else (if x = 1 then 1 else f(x-1)+f(x-2)))) in f n

let rec f = (\x. (if x = 0 then 0 else (if x = 1 then 1 else f(x-1)+f(x-2)))) in f 14

;;

// length

// let rec f = (\x. (if (tl x = #) then 0 else 1))) in f a:b:c:#

let rec f = (\x. (if (hd x) = # then 0 else 1+f(tl x))) in f a:#

;;

// even 

let rec f = (\x. (if (hd x) = # then 1 else 1-f(tl x))) in f a:b:c:d:e:f:#

;;

// code to open quiz = concurrency